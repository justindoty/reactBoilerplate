var React = require('react');

var WeatherMessage = React.createClass({
  render: function () {
    return (
      <h3>It's Finally cooling down!</h3>
    );
  }
});

module.exports = WeatherMessage;
